[project]
name = "telegram-media-manager"
version = "2.2.0"
description = "Self-hosted Telegram media downloader with a web panel, resumable downloads and a bundled proxy."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Ethan Zou" }]
keywords = ["telegram", "downloader", "self-hosted", "docker", "mihomo", "nas"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
# Put the repo root on sys.path so `import app` works no matter how pytest is
# invoked (plain `pytest` doesn't add CWD the way `python -m pytest` does).
pythonpath = ["."]

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
